> 适用技术栈：Electron + TypeScript + React 19 + antd 5.x + Ant Design Pro
> 用途：优化提示词，直接粘贴给 AI 获取可落地的 UI 优化方案（粘贴时本说明块可删除）

---

请使用**中文**输出。请作为一位资深的产品 UI/UX 设计师兼前端工程师，对我现有的一款**复式记账（Double-Entry Bookkeeping）桌面应用**进行全面的 UI 优化，输出一份可直接落地的优化方案。

### 〇、交付形态与你的视角（先读）

- **如果你能查看本项目代码库：** 先通读相关页面、组件与现有实现，再给出方案；"现状问题"基于实际代码判断，无需标注 `[假设]`。
- **如果你无法查看代码库：** 本次交付**优化方案文档 + 可复用的示例代码**，不要求直接修改项目代码；涉及项目实际情况的"现状问题"类判断，请基于行业常见情况给出，并**标注 `[假设]`**，便于我对照修正。
- 下文所有"不修改 / 不破坏"类约束，约束的是你给出的方案（或你实施的改动）：不得改动数据库结构（可以增加字段、不能删除字段）、已有 API、复式记账核心逻辑，不得删除已有功能。

### 一、项目背景与技术栈

- **应用类型：** 复式记账应用，面向个人/小微企业用户，核心功能包括：会计科目管理、记账凭证（会计分录）录入、总账/明细账查询、试算平衡、财务报表（资产负债表、利润表、现金流量表）。
- **多账本能力：** 用户可维护多个独立账本（如不同公司/项目/年度），可在账本间快速切换，并保证各账本数据相互隔离、互不串扰。
- **技术栈（必须严格遵守）：**
  - Electron（桌面端）
  - TypeScript
  - React 19（注意：antd 5.x 在 React 19 下需引入 `@ant-design/v5-patch-for-react-19` 兼容补丁，方案中的示例代码需包含此前提）
  - Ant Design（antd）5.x
  - Ant Design Pro：项目已在使用，ProLayout / ProTable 等 Pro 组件**可用且优先考虑**
- **本次唯一允许新增的依赖：** `react-router-dom`，采用 **HashRouter** 模式。原因：Electron 以 `file://` 协议加载页面，BrowserRouter 在该环境下不可用，HashRouter 是桌面端标准做法。路由化属于**纯 UI 层改造**，不得借路由改动引入业务逻辑或数据结构变化。
- **术语约定（你的方案与全文必须统一遵循）：**

  | 导航命名 | 领域术语 | 说明 |
  | --- | --- | --- |
  | 交易 | 记账凭证（会计分录） | 指凭证的录入、查询与管理 |
  | 账户 | 会计科目 | 指科目（含科目分类）管理 |
  | 分类 | 科目分类 | 并入"账户（科目管理）"页，以科目类型/分类维度呈现，不单独设页 |
  | 对账 | 总账/明细账/科目余额表查询 | 银行对账如需要也归入此页 |

- **当前现状：** 已有可用的基础功能，但界面"像后台管理系统"，缺乏财务软件应有的专业感、层次感和操作效率。

### 二、优化目标

在不改变现有业务逻辑与数据模型的前提下，对 UI 层进行优化，目标是：

1. **提升专业感与可信度**——让产品看起来像一款成熟的财务软件，而非普通 CRUD 后台。
2. **降低记账操作门槛**——新用户无需财务背景也能快速上手录入凭证。
3. **强化数据可读性**——让借贷关系、科目余额、报表数字一目了然。
4. **统一视觉规范**——颜色、间距、字体、组件用法一致。
5. **充分利用 Electron 桌面窗口空间**——避免将桌面端做成"窄屏移动端式"布局，合理利用窗口宽度、高度与多列能力。
6. **优化冷启动体验**——SQLite 账本数据库（复式记账数据）加载期间不得出现长时间白屏，启动全程有清晰的视觉反馈与加载节奏。

### 三、需要优化的具体模块（请逐项给出方案）

**优先级：P0 = 模块 1、3、8；P1 = 模块 2、5、7、9；P2 = 模块 4、6。请按 P0 → P1 → P2 的顺序输出。**

请针对以下模块，分别给出**视觉设计建议 + 布局结构建议 + 具体 antd / Pro 组件选型与用法**：

1. **全局框架与导航（P0，重点）**
   - 建议整体采用如下布局（作为基线方案，若与现有结构冲突可做取舍调整）：
     ```
     ┌─────────────────────────────────────────┐
     │ 顶部标题栏 / 全局操作区                  │
     ├──────────────┬──────────────────────────┤
     │              │                          │
     │   左侧导航   │       主内容区域          │
     │              │                          │
     │   总览       │                          │
     │   交易       │                          │
     │   账户       │                          │
     │   对账       │                          │
     │   报表       │                          │
     │              │                          │
     │   设置       │                          │
     │              │                          │
     └──────────────┴──────────────────────────┘
     ```
     （原"分类"按术语约定并入"账户"页；若你认为保留独立导航项更优，需给出理由与方案。）
   - 顶部标题栏：指**系统原生标题栏下方的应用内全局操作区**（非 frameless 自定义标题栏），承载全局操作（**账本切换为主入口，见模块 8**、全局搜索、新建记账、同步/导出、用户设置等）。若你认为 frameless 自定义标题栏体验更优，可作为附加方案单独简述，但默认按应用内 Header 设计。
   - 左侧导航：总览、交易、账户、对账、报表、设置；说明各导航项的菜单分组逻辑、当前项高亮与面包屑。
   - **路由方案（本次新增，重点）：** 基于 `react-router-dom` 的 HashRouter 完成路由化设计，需给出：
     - 完整路由表：每个路由的 path、对应页面、对应左侧菜单项（含菜单高亮映射）；
     - `Menu` 的 selectedKeys / openKeys 与 `location.pathname` 的同步方式；
     - 路由切换时仅替换主内容区，顶部标题栏与左侧导航不重载、不闪烁；
     - 路由切换时主内容区滚动位置复位到顶部；
     - **路由切换动画：** 页面跳转时主内容区域需有过渡动画（淡入/轻微位移等，建议 150-250ms），用**纯 CSS** 实现（如以 `location.pathname` 作为 key 重新挂载内容容器并施加 animation class），不新增依赖；仅主内容区域参与动画，顶部标题栏与左侧导航保持静止；动画不得阻塞滚动复位与数据加载；需遵循系统"减弱动态效果"偏好（`prefers-reduced-motion`）；
     - 当前账本上下文在路由间的保持方式（全局状态管理 vs `/ledger/:ledgerId/...` 前缀式路由），给出两种取舍与你的推荐；
     - 默认路由与 404：进入应用默认跳转总览页，未知路径重定向。
   - **如果当前项目中缺少上述页面/模块（例如"对账"页面不存在），允许你进行添加补齐**，并充分利用 Electron 桌面窗口空间设计相应页面。
   - **滚动行为要求：** 仅"主内容区域"内部滚动（自带滚动条）；滚动时顶部标题栏与左侧菜单保持固定、不得被带动滚动，整体 Layout 不发生滚动。
   - **菜单收起/展开：** 左侧菜单栏支持收起与展开（收起后仅显示图标，展开时显示完整菜单文字），收起/展开后主内容区域宽度需随之自适应调整。
   - **自适应要求：** 整体界面需具备自适应能力，窗口缩放时主内容区域弹性伸缩、布局不错乱、表格合理适配宽度。窗口尺寸按**默认 1440×900、最小 1280×800** 设计。
2. **仪表盘（总览页）（P1）**
   - 关键财务指标卡片（如总资产、总负债、净资产、本月收支）的布局与可视化方式。
   - **时间粒度与范围筛选（新增）：** 指标卡与图表支持按**年 / 月 / 周 / 日**粒度切换，并支持**自定义时间范围**；请给出默认展示口径的建议。
   - **同比对比（新增）：** 支持与**去年同期对比**——趋势图叠加去年同期曲线、指标卡展示同比增减幅度（涨跌配色遵循模块 7 的色彩约定）。
   - **图表筛选（新增）：** 页面内所有图表统一复用同一套时间筛选组件与规则（与模块 3 交易流水的筛选规则保持一致），不允许各图表各自为政。
3. **记账凭证录入页（交易页）（P0，重点）**
   - 借贷双方分录的表格/表单布局、科目选择器（支持搜索与树形结构）、金额对齐方式、借贷平衡实时校验提示、常用凭证模板。
   - **交易流水历史列表（新增）：** 默认按时间**倒序**（最新在前）展示流水/凭证记录；提供时间范围筛选：**今日 / 本周 / 近七天 / 本月 / 自定义时间范围**；需明确列表关键字段（日期、凭证号、摘要、借方、贷方等）与分页/虚拟滚动方案。
4. **科目（账户）管理（P2）**
   - 科目树/列表的展示（含科目分类维度）、期初余额设置、启用/停用状态的可视化。
5. **账表查询页（对账）（P1）**
   - 总账、明细账、科目余额表、银行对账的数据表格设计：列宽、数字右对齐、千分位、负数红色显示、冻结表头、分页与筛选。
6. **财务报表（报表页）（P2）**
   - 资产负债表、利润表、现金流量表的排版格式（贴近真实财务报表格调）：资产负债表采用**账户式**（左方资产、右方负债+所有者权益），利润表与现金流量表采用**报告式**（上下结构）。
7. **配色与视觉系统（P1）**
   - 建议一套主色调/功能色（借贷常用色、成功/警告/错误色）、明暗两种主题的处理方式（基于 antd 5 `theme.algorithms` 的 default / dark 算法）、字号与间距规范。
   - **色盲安全：** 借贷方向**不得仅靠红绿区分**；红色仅用于表示负数/赤字；关键功能色需满足 WCAG AA 对比度要求。
8. **账本目录切换（P0，重点）**
   - 多账本（账簿）管理入口：多个账本（如不同公司/项目/年度）的创建、重命名、归档、删除的入口设计。
   - **切换交互方案：** 主入口已确定放在**顶部全局操作区**（见模块 1），请给出顶部入口的具体交互设计（当前账本名展示 + 下拉切换的组件选型与细节）；另对比 1-2 种**辅助**切换方式（如搜索式快捷切换弹层/抽屉、最近使用账本）作为补充，并说明取舍。
   - 当前账本的可视化标识：全局高亮当前账本名称、切换后页面状态（科目/凭证/报表）如何随之刷新与清空。
   - **与路由方案的联动：** 切换账本后当前路由保持不变、页面数据按新账本刷新（与模块 1 的路由方案保持一致）。
   - 账本快捷访问：最近使用账本、置顶/收藏账本、账本排序规则。
   - 切换防误操作：未保存的凭证、切换确认提示、账本间数据隔离的明确提示。
   - 数据隔离与权限边界：切换账本后，科目树、凭证列表、报表数据严格跟随当前账本，无串数据。
9. **冷启动与加载体验（P1，新增）**
   - **白屏治理：** 针对"启动 → 打开 SQLite 账本数据库 → 首屏可交互"的冷启动链路，给出消除长时间白屏的方案：窗口创建后立即显示与主题背景色一致的启动画面/Splash（或先 show 窗口 + 底色占位），数据就绪后平滑过渡到首屏。
   - **加载态分级：** 区分三种加载状态并给出组件选型——启动 Loading（Splash/Progress）、骨架屏（Skeleton 覆盖指标卡/流水列表/图表区域）、局部加载（Spin/按钮 loading）；避免整页转圈和数据就绪后布局跳变。
   - **渐进式上屏：** 首屏框架（导航 + 布局）不等待数据、立即可见；指标卡、流水列表、图表按数据就绪顺序渐进填充，SQLite 查询期间对应区域显示 Skeleton 占位。
   - **失败兜底：** 数据库打开/初始化失败时的错误态 UI（antd `Result`/`Alert` + 重试入口），不得静默卡死在白屏。
   - **性能建议边界：** 可给出启动性能优化建议（如主进程提前建立 SQLite 连接、仅按当前账本按需加载、避免启动时全量加载所有账本数据、数据加载与渲染首屏并行）；此类建议**单独列出**，并标注哪些属于 UI 层、哪些超出 UI 层需我另行决策；不得要求修改数据库结构、已有 API 与复式记账核心逻辑。
   - **量化验收：** 给出冷启动体验的可量化指标建议（如骨架屏出现时间、首屏可交互时间、白屏时长上限）。

### 四、必须遵守的约束

- 必须基于 **antd 5.x 组件**实现（如 Layout、Menu、Table、Form、Select、TreeSelect、Statistic、Tabs、Card、Empty、Badge 等），优先使用官方组件，避免大量自造轮子。
- **ProLayout 优先：** 项目已使用 Ant Design Pro，固定头部、侧边导航、菜单收起展开、内容区独立滚动等**优先采用 ProLayout 能力实现**；ProLayout 覆盖不了的场景再退回 antd `Layout` + `Sider` 固定、`Layout.Content` 内部 `overflow: auto` 的方案。数据表格优先考虑 ProTable。请对"用 Pro 组件还是纯 antd 组件"给出取舍理由。
- 给出**具体的 CSS-in-JS / less 主题定制建议**（antd 5 的 Design Token），如颜色、圆角、字号等。
- 数字类内容统一**右对齐**，金额支持**千分位分隔**与**负号/红色显示**规则（明确借贷方向与正负的展示约定）。
- 保持 TypeScript 类型安全，组件 props 与状态要有清晰的类型定义建议。
- 考虑 Electron 桌面端的窗口尺寸适配（默认 1440×900、最小 1280×800；最小宽度下的布局降级、列表自适应、避免横向滚动条过多）。
- **页面补全：** 若现有项目缺少"总览/交易/账户/对账/报表/设置"中的任一页面，可自行补充设计与实现方案；在**充分利用 Electron 桌面窗口空间**（合理布局、多列展示、表格密度）的前提下补齐页面结构，不得以"页面不存在"为由跳过。
- **滚动与布局结构：** 主内容区域使用独立滚动容器（仅内容区出现滚动条），顶部标题栏与左侧菜单固定不随滚动移动；整体 Layout 不被内容滚动带动。
- **菜单可收起：** 左侧菜单支持收起/展开，处理好收起态下图标提示（Tooltip）与主内容区宽度联动。
- **路由约束（新增）：** 路由化仅允许新增 `react-router-dom` 一个依赖，且必须使用 **HashRouter**；路由只负责页面组织与导航状态，**不得**借路由改动引入业务逻辑变更、数据请求逻辑变更或数据结构变化；顶部标题栏与左侧导航在路由切换时不重载；路由切换动画须以**纯 CSS** 实现，不得为此引入 framer-motion、react-transition-group 等动画库。
- **依赖纪律：** 除上述 `react-router-dom` 外**零新增依赖**；如你认为某项功能必须新增依赖，请单独列出并说明理由，由我决定是否采纳。
- **输出纪律：** 全文中文；每个模块的关键代码示例**不超过 30 行**，只给结构性骨架，以方案为主、代码为辅；禁止"提升用户体验""更加美观"这类空泛表述，所有建议必须落到具体组件、属性、Design Token 或交互规则。

### 五、交付格式与要求

请按以下结构输出：

1. **整体优化思路**（200 字内概述核心设计原则）。
2. **视觉规范建议**（配色方案、Design Token 表格、字号/间距、明暗主题；后续所有模块引用该 Token 表，不再重复定义）。
3. **路由表**（新增）：path ↔ 页面 ↔ 菜单项的完整映射表，含默认路由与重定向规则。
4. **分模块优化方案**（按 P0 → P1 → P2 顺序，对第三部分的每个模块逐一给出：现状问题（能查看代码则基于实际；否则标注 `[假设]`） → 优化建议 → 组件选型 → 关键代码/结构示例 ≤30 行）：
   - 模块 1-7、9 按通用格式输出；
   - **模块 8（账本目录切换）** 单独输出：现状问题 → 切换交互方案对比 → 组件选型 → 关键代码/结构示例 → 防误操作设计。
5. **交互细节清单**（列出 5-10 条提升体验的细节，如空状态、加载态、防误操作、快捷键、表单校验反馈等）。
6. **可优先落地的 Quick Wins**（按投入产出比排序，先列最容易见效的 3-5 项）。

代码示例使用 **TypeScript + React 19 + antd 5** 语法，并保持可直接复用。

### 六、硬性红线

* 不破坏现有业务逻辑
* 不修改数据库结构、可以增加不能删除字段
* 不修改已有 API
* 不修改复式记账核心逻辑
* 不随意删除已有功能
* 尽可能复用已有组件
* 避免重复代码
* 保持 TypeScript 类型安全
* 路由化仅限 UI 层：仅新增 `react-router-dom`，必须使用 HashRouter
